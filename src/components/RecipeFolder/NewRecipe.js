import React from 'react'
import {Form, Button} from 'react-bootstrap'
import { useState } from 'react'


// interface Food {
//     id: String;
//     ingredient: String;
// }

const NewRecipe = ({recipe, setRecipe}) => {

    // const[ingredients, setIngredients] = useState<Food[]>([{id: "5", ingredient: "apples"}])


  return (
    <div>
       {/* {ingredients.map((i)=>{
           return(
                <div key={i.id}>
                   <input placeholder='enter ingredient'/>
                </div>
           )
       })} */}
        
    
    </div>
  )
}

export default NewRecipe